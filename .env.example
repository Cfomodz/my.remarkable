# Get a one-time code from https://my.remarkable.com/device/browser/connect
# Paste it here, then run the server — it will exchange it for a device token.
REMARKABLE_ONE_TIME_CODE=
# Once registered, the device token is stored automatically. You can also set it directly:
REMARKABLE_DEVICE_TOKEN=
